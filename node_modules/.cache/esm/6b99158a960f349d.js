let User,jwt,bcrypt;_960‍.x([["invalidSigninResponse",()=>invalidSigninResponse]]);_960‍.w("../models/userModel",[["default",["User"],function(v){User=v}]]);_960‍.w("jsonwebtoken",[["*",null,function(v){jwt=v}]]);_960‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);



const ROUNDS = 10;

module.exports = {
  registerUser: async (req, res) => {
    try {
      const task = req.body;

      const user = req.body;
      const userCreated = await User.create ({
        email: user.email,
        name: user.name,
        password: bcrypt.hashSync (user.password, ROUNDS),
      });

      userCreated
        ? res.status (201).send ({message: 'Usuário criado'})
        : res.status (500).send ({error: 'Falha ao criar usuárior'});
    } catch (error) {
      _960‍.g.console.error (error);
      res.status (500).json ({message: 'Não foi possivel cadastrar o usuário'});
    }
  },
  signin: async (req, res) => {
    try {
      const {email, password} = req.body;

      User.findOne ({email}, async (err, user) => {
        _960‍.g.console.log (user);
        if (user) {
          const isValidPassword = bcrypt.compareSync (password, user.password);

          const jwtToken = await jwt.sign (
            {
              user: {
                id: user.id,
                email: user.email,
                name: user.name,
              },
            },
            process.env.JWT_SECRET,
            {
              expiresIn: '30 days',
            }
          );

          isValidPassword
            ? res.status (200).json ({jwtToken})
            : invalidSigninResponse;
        } else {
          invalidSigninResponse;
        }
      });
    } catch (error) {
      _960‍.g.console.error (error);
      res.send (500).json ('Não foi possível fazer login do usuário');
    }
  },
};

       const invalidSigninResponse = (req, res) => {
  res.status (401).json ({message: 'Email ou senha inválidos'});
};
