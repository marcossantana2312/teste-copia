let express,bodyParser;_01f‍.w("express",[["default",["express"],function(v){express=v}]]);_01f‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);

const router = require("./routes");
const connectDb = require("./mongoose")

const PORT = process.env.PORT || 4000;
const app = express();

app.use(bodyParser.json());
app.use(router);

_01f‍.g.console.log(process.env.DATABASE_URL);

connectDb().then(async () => {
    app.listen(PORT, () => {
        _01f‍.g.console.log("Server listenin on port " + PORT);
    })
});